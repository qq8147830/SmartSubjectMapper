# 智能学科定位器 - 完整部署指南

## 🚀 项目状态
✅ 项目代码已完成
✅ TypeScript检查通过
✅ 构建测试成功
✅ 部署配置文件已准备

## 📋 部署前准备

### 1. 获取必要的API密钥

**Supabase配置：**
1. 访问 [https://supabase.com](https://supabase.com) 并登录
2. 创建新项目（如果还没有的话）
3. 在项目设置中获取：
   - Project URL (格式: `https://xxx.supabase.co`)
   - anon public key

**SiliconFlow AI API配置：**
1. 访问 [https://siliconflow.cn](https://siliconflow.cn)
2. 注册/登录账号
3. 在控制台中创建API Key

### 2. 本地测试（可选）

在项目根目录更新 `.env` 文件：
```env
VITE_SUPABASE_URL=你的Supabase项目URL
VITE_SUPABASE_ANON_KEY=你的Supabase匿名密钥
VITE_QWEN_API_KEY=你的通义千问API密钥
```

然后运行：
```bash
npm run dev
```

## 🌐 Vercel部署步骤

### 步骤1: 准备Vercel账号
1. 访问 [https://vercel.com](https://vercel.com)
2. 使用GitHub账号登录（推荐）

### 步骤2: 部署方法

**方法A: 通过Vercel CLI（推荐）**

1. 在项目目录中运行：
   ```bash
   npm install -g vercel
   ```

2. 登录Vercel：
   ```bash
   vercel login
   ```

3. 部署项目：
   ```bash
   vercel --prod
   ```

4. 按照提示操作：
   - 确认项目设置
   - 设置环境变量（见下方）
   - 确认部署

**方法B: 通过GitHub + Vercel**

1. 将项目推送到GitHub仓库
2. 在Vercel网站中连接GitHub仓库
3. 设置构建设置（自动检测）

### 步骤3: 配置环境变量

在Vercel部署过程中或部署后，在项目设置中添加以下环境变量：

| 变量名 | 值 | 说明 |
|--------|----|------|
| `VITE_SUPABASE_URL` | 你的Supabase项目URL | 如: `https://abc123.supabase.co` |
| `VITE_SUPABASE_ANON_KEY` | 你的Supabase匿名密钥 | 以`eyJ`开头的长字符串 |
| `VITE_AI_BASE_URL` | `https://api.siliconflow.cn/v1` | SiliconFlow API基础URL |
| `VITE_AI_MODEL_ID` | `Qwen/Qwen2.5-7B-Instruct` | AI模型ID |
| `VITE_AI_API_KEY` | 你的SiliconFlow API密钥 | SiliconFlow的API密钥 |

**设置路径：**
- Vercel项目 → Settings → Environment Variables

### 步骤4: 配置Supabase数据库

1. 登录你的Supabase项目
2. 进入SQL Editor
3. 运行以下SQL创建必要的表结构：

```sql
-- 用户资料扩展表
CREATE TABLE IF NOT EXISTS user_profiles (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id UUID REFERENCES auth.users(id) ON DELETE CASCADE,
  display_name TEXT,
  phone TEXT,
  location TEXT[],
  child_age TEXT,
  education TEXT,
  bio TEXT,
  created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
  updated_at TIMESTAMP WITH TIME ZONE DEFAULT NOW()
);

-- 学习分析记录表
CREATE TABLE IF NOT EXISTS learning_records (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id UUID REFERENCES auth.users(id) ON DELETE CASCADE,
  content TEXT NOT NULL,
  analysis JSONB NOT NULL,
  created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW()
);

-- 学习路径表
CREATE TABLE IF NOT EXISTS learning_paths (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id UUID REFERENCES auth.users(id) ON DELETE CASCADE,
  title TEXT NOT NULL,
  description TEXT,
  subjects JSONB NOT NULL,
  status TEXT DEFAULT 'active',
  created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
  updated_at TIMESTAMP WITH TIME ZONE DEFAULT NOW()
);

-- 用户设置表
CREATE TABLE IF NOT EXISTS user_settings (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id UUID REFERENCES auth.users(id) ON DELETE CASCADE,
  email_notifications BOOLEAN DEFAULT true,
  study_reminders BOOLEAN DEFAULT true,
  data_sync BOOLEAN DEFAULT true,
  theme TEXT DEFAULT 'light',
  created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
  updated_at TIMESTAMP WITH TIME ZONE DEFAULT NOW()
);

-- 创建索引
CREATE INDEX IF NOT EXISTS idx_user_profiles_user_id ON user_profiles(user_id);
CREATE INDEX IF NOT EXISTS idx_learning_records_user_id ON learning_records(user_id);
CREATE INDEX IF NOT EXISTS idx_learning_paths_user_id ON learning_paths(user_id);
CREATE INDEX IF NOT EXISTS idx_user_settings_user_id ON user_settings(user_id);

-- 启用行级安全
ALTER TABLE user_profiles ENABLE ROW LEVEL SECURITY;
ALTER TABLE learning_records ENABLE ROW LEVEL SECURITY;
ALTER TABLE learning_paths ENABLE ROW LEVEL SECURITY;
ALTER TABLE user_settings ENABLE ROW LEVEL SECURITY;

-- 创建RLS策略
CREATE POLICY "Users can view own profile" ON user_profiles FOR SELECT USING (auth.uid() = user_id);
CREATE POLICY "Users can insert own profile" ON user_profiles FOR INSERT WITH CHECK (auth.uid() = user_id);
CREATE POLICY "Users can update own profile" ON user_profiles FOR UPDATE USING (auth.uid() = user_id);

CREATE POLICY "Users can view own records" ON learning_records FOR SELECT USING (auth.uid() = user_id);
CREATE POLICY "Users can insert own records" ON learning_records FOR INSERT WITH CHECK (auth.uid() = user_id);
CREATE POLICY "Users can update own records" ON learning_records FOR UPDATE USING (auth.uid() = user_id);
CREATE POLICY "Users can delete own records" ON learning_records FOR DELETE USING (auth.uid() = user_id);

CREATE POLICY "Users can view own paths" ON learning_paths FOR SELECT USING (auth.uid() = user_id);
CREATE POLICY "Users can insert own paths" ON learning_paths FOR INSERT WITH CHECK (auth.uid() = user_id);
CREATE POLICY "Users can update own paths" ON learning_paths FOR UPDATE USING (auth.uid() = user_id);
CREATE POLICY "Users can delete own paths" ON learning_paths FOR DELETE USING (auth.uid() = user_id);

CREATE POLICY "Users can view own settings" ON user_settings FOR SELECT USING (auth.uid() = user_id);
CREATE POLICY "Users can insert own settings" ON user_settings FOR INSERT WITH CHECK (auth.uid() = user_id);
CREATE POLICY "Users can update own settings" ON user_settings FOR UPDATE USING (auth.uid() = user_id);
```

## ✅ 部署验证

部署成功后，请验证以下功能：

### 1. 基础功能测试
- [ ] 页面正常加载，无JavaScript错误
- [ ] 用户注册功能正常
- [ ] 用户登录功能正常
- [ ] 导航菜单工作正常

### 2. 核心功能测试
- [ ] 智能分析页面可以输入内容
- [ ] AI分析功能返回结果
- [ ] 学习仪表板显示图表
- [ ] 个人资料编辑功能正常
- [ ] 学习记录保存和显示

### 3. 数据持久化测试
- [ ] 注册后用户信息正确保存
- [ ] 分析结果可以查看历史记录
- [ ] 个人设置可以保存和恢复

## 🔧 故障排除

### 常见问题

**Q: 页面显示空白**
A: 检查浏览器控制台错误，通常是环境变量配置问题

**Q: 登录功能异常**
A: 确认Supabase项目URL和API密钥正确

**Q: AI分析不工作**
A: 验证通义千问API密钥是否有效

**Q: 图表不显示**
A: 检查网络连接，刷新页面重试

### 调试步骤

1. 打开浏览器开发者工具（F12）
2. 查看Console选项卡是否有错误
3. 查看Network选项卡检查API请求
4. 验证环境变量是否正确加载

## 📞 技术支持

如果在部署过程中遇到问题：

1. 检查本指南的故障排除部分
2. 确认所有API密钥配置正确
3. 验证Supabase数据库表结构是否正确创建
4. 检查Vercel部署日志

## 🎯 部署完成后的下一步

1. **功能验证**：全面测试所有功能
2. **用户反馈**：收集早期用户反馈
3. **性能优化**：根据使用情况优化加载速度
4. **功能迭代**：根据用户需求添加新功能

---

**部署完成后，您将拥有一个完整的智能学科定位器应用！** 🎉